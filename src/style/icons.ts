import { LabIcon } from '@jupyterlab/ui-components';

// icon svg import statements
import pullRequest from '../../style/icons/pullrequest.svg';

export const pullRequestsIcon = new LabIcon({
  name: 'pullrequests:icon',
  svgstr: pullRequest
});
