import * as samplepr from './samplepr.json';
import * as samplefile from './samplefile.json';

export let SAMPLE_PR_JSON: string = JSON.stringify(samplepr);
export let SAMPLE_FILE_JSON: string = JSON.stringify(samplefile);